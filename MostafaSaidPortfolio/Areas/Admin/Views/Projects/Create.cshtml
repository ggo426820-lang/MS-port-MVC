@model MostafaSaidPortfolio.Domain.Entities.Project
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "New Project";
    var categories = ViewData["Categories"] as IEnumerable<MostafaSaidPortfolio.Domain.Entities.Category>
                     ?? Enumerable.Empty<MostafaSaidPortfolio.Domain.Entities.Category>();
}

<div class="flex items-center justify-between mb-6">
    <h2 class="text-xl font-bold text-white">New Project</h2>
    <a href="/Admin/Projects" class="btn-ghost">← Back</a>
</div>

<form asp-action="Create" method="post" class="space-y-6">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id"/>

    <div class="stat-card space-y-4">
        <div>
            <label class="form-label">Title *</label>
            <input asp-for="Title" class="form-input" required/>
        </div>
        <div>
            <label class="form-label">Slug (auto-generated if blank)</label>
            <input asp-for="Slug" class="form-input font-mono text-sm"/>
        </div>
        <div>
            <label class="form-label">Short Description *</label>
            <input asp-for="Description" class="form-input" placeholder="One-liner for cards" required/>
        </div>
        <div>
            <label class="form-label">Long Description</label>
            <textarea asp-for="LongDescription" class="form-input" rows="4" placeholder="Detailed project description"></textarea>
        </div>
        <div>
            <label class="form-label">Technology Stack *</label>
            <input asp-for="TechnologyStack" class="form-input" placeholder="ASP.NET Core, PostgreSQL, Tailwind CSS" required/>
        </div>
    </div>

    <div class="stat-card space-y-4">
        <h3 class="font-semibold text-white">Links & Media</h3>
        <div class="grid sm:grid-cols-2 gap-4">
            <div>
                <label class="form-label">Category</label>
                <select asp-for="CategoryId" class="form-input">
                    <option value="">— None —</option>
                    @foreach (var cat in categories)
                    {
                        <option value="@cat.Id">@cat.Name</option>
                    }
                </select>
            </div>
            <div>
                <label class="form-label">Display Order</label>
                <input asp-for="DisplayOrder" type="number" min="0" class="form-input"/>
            </div>
            <div>
                <label class="form-label">GitHub URL</label>
                <input asp-for="GitHubUrl" class="form-input" placeholder="https://github.com/..."/>
            </div>
            <div>
                <label class="form-label">Live URL</label>
                <input asp-for="LiveUrl" class="form-input" placeholder="https://..."/>
            </div>
            <div>
                <label class="form-label">Image URL</label>
                <input asp-for="ImageUrl" class="form-input" placeholder="https://..."/>
            </div>
            <div>
                <label class="form-label">Thumbnail URL</label>
                <input asp-for="ThumbnailUrl" class="form-input" placeholder="https://..."/>
            </div>
        </div>
        <div class="flex items-center gap-2">
            <input asp-for="IsFeatured" type="checkbox" id="IsFeatured" class="rounded"/>
            <label for="IsFeatured" class="text-sm text-slate-300">Featured project</label>
        </div>
    </div>

    <div class="flex gap-3">
        <button type="submit" class="btn-primary">Create Project</button>
        <a href="/Admin/Projects" class="btn-ghost">Cancel</a>
    </div>
</form>
